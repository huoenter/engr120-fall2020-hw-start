/* Returns the sum of a and b */
int add(int a, int b) {
    return -1;
}

/* Returns the sum of numbers in [lower, upper]
 * Return 0 when lower > upper
 * For example, square_sum(1, 3) = 1 + 2 + 3 = 6
 */
int range_sum(int lower, int upper)
{
    return -1;
}

/* Returns the sum of squared numbers in [lower, upper]
 * Return 0 when lower > upper
 * For example, square_sum(1, 3) = 1^2 + 2^2 + 3^2 = 14
 */
int square_sum(int lower, int upper)
{
    return -1;
}

/* Returns the fare of a taxi trip
 * miles: miles of the trip
 * rate: price per mile
 * There is also a fixed $5 for every trip no matter the mileage
 * For example, the fare for 1.8 miles at $2.70 costs
 * 5 + 1.8 * 2.7 = 9.86
 * Assume inputs are postive and valid
 */
double fare(double miles, double rate)
{
    return -1.0;
}
